{
  "name": "trader-economics",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn check-types && npx ts-node src/index.ts",
    "deploy": "yarn build && npx ts-node src/deploy.ts",
    "clear": "rm -rf dist",
    "check-types": "tsc --noEmit"
  },
  "author": "Antroll",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^22.10.1",
    "fs": "^0.0.1-security",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
